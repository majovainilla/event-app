<h1>Events</h1>
<ul class="card"> <% @events.each do |event| %>
      <li id='event-<%= event.id %>'>
        <article class="card-body">
        <div class="card-title"><%= event.name %></div>
        <div class="card-text"><%= event.description %></div>
        <div class="data-card">
          <div class="timestamp">
            Created <%= time_ago_in_words(event.created_at) %>  ago by:
          </div>
        </div>
        </article>
      </li>
    <%end%>
    </ul>